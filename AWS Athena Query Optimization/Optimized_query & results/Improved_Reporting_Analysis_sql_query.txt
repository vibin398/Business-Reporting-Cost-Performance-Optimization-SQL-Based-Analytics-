SELECT
    order_year,
    order_month,
    COUNT(*) AS order_count,
    SUM(total_amount) AS total_sales
FROM investigation.orders_parquet
WHERE status = 'Completed'
  AND order_year = 2025
GROUP BY order_year, order_month
ORDER BY order_month;
